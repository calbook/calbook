<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.calbook.web.dao.GroupsDAO">
	
	<!-- create a group  -->
	<insert id="addGroup">
		INSERT INTO GROUPS (NUM, OWNER, CONTENT, NAME) VALUES((SELECT MAX(NUM+1) FROM GROUPS),#{owner},#{content},#{name})
	</insert>
	
	
</mapper>