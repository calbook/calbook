<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.calbook.web.dao.SchedulesDAO">
	<!-- 일정 가져오기 -->
	<select id="getSchedulesDates" resultType="com.calbook.web.vo.Schedules">
		SELECT * FROM SCHEDULES WHERE M_EMAIL = #{param1} AND (START_DATE BETWEEN #{param2} AND #{param3} OR FINISH_DATE BETWEEN #{param2} AND #{param3})
	</select>

	<select id="getSchedulesMonth" resultType="com.calbook.web.vo.Schedules">
		SELECT * FROM SCHEDULES WHERE M_EMAIL = #{param1} AND START_DATE LIKE '${param2}%' AND FINISH_DATE LIKE '${param2}%'
	</select>
	
	
</mapper>